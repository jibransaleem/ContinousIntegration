name: YAML_7_PRAC

on:
  push:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Print secret
      - env :
        My_env : ${{ secrets.MY_SECRET }}
      
        run: |
         echo : "$My_env"
